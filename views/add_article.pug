extends layout
block content
    h1 #{title}
    form(method="post", action="/articles/add")
        #form-group
            label Title
            input.form-control(name="title",type="text")
        #form-group
            label Auther
            input.form-control(name="auther",type="text")
        #form-group
            label Body
            textarea.form-control(name="body")
        br
        input.btn.btn-primary(value="Submit",type="submit")